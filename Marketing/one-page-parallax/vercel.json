{
  "version": 2,
  "name": "studiox-marketing",
  "buildCommand": "npm install && npx gulp font-fontawesome && npx gulp font-bootstrap && npx gulp css-vendor && npx gulp css-app && npx gulp js-vendor && npx gulp js-app && npx gulp plugins && npx gulp fileinclude",
  "outputDirectory": "../../template/template_one_page_parallax",
  "installCommand": "npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}

